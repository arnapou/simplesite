stages:
  - quality
  - artifact
  - release

.common-job: &common-job
  image: &php-image 'registry.gitlab.com/arnapou/docker/php:8.2-dev'
  only: [ main, tags ]
  before_script: [ make install ]
  cache:
    key: "$CI_COMMIT_REF_SLUG:$CI_JOB_NAME"
    paths: [ vendor ]

code-style:
  <<: *common-job
  stage: quality
  script: [ make cs ]

static-analysis:
  <<: *common-job
  stage: quality
  script: [ make sa ]

#test:
#  <<: *common-job
#  stage: quality
#  script: [ sudo -Eu www-data make test ]
#
#coverage:
#  <<: *common-job
#  stage: quality
#  script: [ sudo -Eu www-data make coverage ]
#  coverage: '/^\s*Lines:\s*\d+.\d+\%/'

simplesite.phar:
  image: *php-image
  only: [ tags ]
  stage: artifact
  script:
    - echo ARTIFACT_JOB_ID=$CI_JOB_ID >> dotenv.env
    - echo PHP_VERSION=$PHP_VERSION >> dotenv.env
    - make build
  artifacts:
    expire_in: never
    name: "simplesite-$CI_COMMIT_TAG"
    paths: [ bin/simplesite.phar ]
    reports: { dotenv: dotenv.env }

release:
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  stage: release
  only: [ tags ]
  script:
    - echo "Building release $CI_COMMIT_TAG"
  needs:
    - job: simplesite.phar
      artifacts: true
  release:
    name: '$CI_COMMIT_TAG'
    tag_name: '$CI_COMMIT_TAG'
    description: |
      Build of `simplesite.phar` : 
      
      - 🏷️ Tag version $CI_COMMIT_TAG
      - 🐘 PHP version $PHP_VERSION
    assets:
      links:
        - name: 'simplesite.phar'
          url: 'https://gitlab.com/arnapou/simplesite/-/jobs/${ARTIFACT_JOB_ID}/artifacts/file/bin/simplesite.phar'
